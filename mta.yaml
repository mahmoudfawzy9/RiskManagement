_schema-version: '3.1'
ID: RiskManagment
version: 1.0.0
description: A simple CAP project.
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
    - builder: custom
      commands:
        - rm -rf build/dist
        - mkdir -p build/dist
        - rm -rf build/ui-deployer
        - mkdir -p build/ui-deployer
    - builder: custom
      commands:
        - npm install --production
        - >-
          bash -c "cds -v | grep 'cds-dk' || npm i --production --no-save
          @sap/cds-dk @sap/mdk-tools"
        - bash -c "export PATH=$PATH:$(pwd)/node_modules/.bin"
        - cds build --production
modules:
  - name: RiskManagment-RiskManagment.Risks
    type: html5
    path: app/RiskManagment.Risks
    build-parameters:
      builder: custom
      commands:
        - rm -rf dist-zip dist
        - >-
          npx -p @ui5/cli@^3.0.0 ui5 build --dest dist
          --include-task=generateManifestBundle
          --include-task=generateCachebusterInfo
        - mkdir dist-zip
        - bash -c "cd dist && npx bestzip ../dist-zip/RiskManagmentRisks.zip *"
      _artifactName: RiskManagmentRisks.zip
      supported-platforms: []
      build-result: dist-zip
  - name: RiskManagment-db-deployer
    type: hdb
    path: gen/db
    parameters:
      buildpack: nodejs_buildpack
    build-parameters:
      builder: npm
      ignore:
        - node_modules
        - package-lock.json
    requires:
      - name: RiskManagment-service-uaa
      - name: RiskManagment-service-db
  - name: RiskManagment-srv
    type: nodejs
    path: gen/srv
    parameters:
      memory: 256MB
    provides:
      - name: srv-api
        properties:
          srv-url: ${default-url}
    build-parameters:
      builder: npm
      ignore:
        - node_modules
        - package-lock.json
    requires:
      - name: RiskManagment-service-uaa
      - name: RiskManagment-service-db
      - name: RiskManagment-service-destination
      - name: RiskManagment-service-metadata
      - name: RiskManagment-service-connectivity
      - name: RiskManagment-service-logs
  - name: RiskManagment-aux-destinations
    type: com.sap.application.content
    parameters:
      content:
        subaccount:
          existing_destinations_policy: update
          destinations:
            - Authentication: OAuth2UserTokenExchange
              Name: RiskManagment-service-uaa
              ServiceInstanceName: RiskManagment-uaa
              ServiceKeyName: RiskManagment-uaa-key
              sap.cloud.service: lcap.RiskManagment
            - Authentication: OAuth2UserTokenExchange
              Name: RiskManagment-app-srv
              TokenServiceInstanceName: RiskManagment-uaa
              TokenServiceKeyName: RiskManagment-uaa-key
              URL: ~{srv-api/srv-url}
              sap.cloud.service: lcap.RiskManagment
            - Name: RiskManagment-service-html5-apps-repo-host
              ServiceInstanceName: RiskManagment-html5-apps-repo-host
              ServiceKeyName: RiskManagment-html5-apps-repo-host-key
              sap.cloud.service: lcap.RiskManagment
            - Authentication: OAuth2UserTokenExchange
              Name: RiskManagment-RiskManagmentService
              TokenServiceInstanceName: RiskManagment-uaa
              TokenServiceKeyName: RiskManagment-uaa-key
              URL: ~{srv-api/srv-url}/service/RiskManagment
              sap.cloud.service: lcap.RiskManagment
              sap.applicationdevelopment.actions.enabled: false
              sap.build.usage: CAP
              sap.processautomation.enabled: false
              HTML5.DynamicDestination: false
              WebIDEEnabled: false
              MobileEnabled: false
              AppgyverEnabled: false
    build-parameters:
      no-source: true
    requires:
      - name: RiskManagment-service-uaa
        parameters:
          service-key:
            name: RiskManagment-uaa-key
      - name: srv-api
      - name: RiskManagment-service-html5-apps-repo-host
        parameters:
          service-key:
            name: RiskManagment-html5-apps-repo-host-key
      - name: RiskManagment-service-destination
        parameters:
          content-target: true
  - name: RiskManagment-launchpad
    type: html5
    path: build/launchpad
    build-parameters:
      builder: custom
      commands:
        - npm install
        - >-
          npx -p @ui5/cli@^3.0.0 ui5 build --dest dist
          --include-task=generateManifestBundle
          --include-task=generateCachebusterInfo
        - bash -c "cd dist && npx bestzip ../../../build/dist/launchpad.zip *"
        - rm -rf dist
      _artifactName: launchpad.zip
      supported-platforms: []
      build-result: ../../build/dist
  - name: RiskManagment-aux-ui-deployer
    type: com.sap.application.content
    path: build/ui-deployer
    build-parameters:
      build-result: resources
      requires:
        - name: RiskManagment-RiskManagment.Risks
          artifacts:
            - RiskManagmentRisks.zip
          target-path: resources
        - name: RiskManagment-launchpad
          artifacts:
            - launchpad.zip
          target-path: resources
    requires:
      - name: RiskManagment-service-html5-apps-repo-host
        parameters:
          content-target: true
resources:
  - type: org.cloudfoundry.managed-service
    name: RiskManagment-service-uaa
    parameters:
      service: xsuaa
      service-plan: application
      service-name: RiskManagment-uaa
      path: xs-security.json
      config:
        oauth2-configuration:
          redirect-uris:
            - https://*.${default-domain}/login/*
        xsappname: RiskManagment-${space}
        role-collections:
          - description: ''
            name: RiskManagment-RiskViewer-${space}
            role-template-references:
              - $XSAPPNAME.RiskViewer
          - description: RiskManager.
            name: RiskManagment-RiskManager-${space}
            role-template-references:
              - $XSAPPNAME.RiskManager
  - type: org.cloudfoundry.managed-service
    name: RiskManagment-service-db
    parameters:
      service: hana
      service-plan: hdi-shared
      service-name: RiskManagment-db
  - type: org.cloudfoundry.managed-service
    name: RiskManagment-service-destination
    parameters:
      service: destination
      service-plan: lite
      service-name: RiskManagment-destination
      config:
        HTML5Runtime_enabled: true
        version: 1.0.0
  - type: org.cloudfoundry.user-provided-service
    name: RiskManagment-service-metadata
    parameters:
      service: metadata
      service-plan: standard
      service-name: RiskManagment-metadata
      config:
        default-domain: ${default-domain}
  - type: org.cloudfoundry.managed-service
    name: RiskManagment-service-connectivity
    parameters:
      service: connectivity
      service-plan: lite
      service-name: RiskManagment-connectivity
  - type: org.cloudfoundry.managed-service
    name: RiskManagment-service-logs
    parameters:
      service: application-logs
      service-plan: lite
      service-name: RiskManagment-logs
  - type: org.cloudfoundry.managed-service
    name: RiskManagment-service-html5-apps-repo-host
    parameters:
      service: html5-apps-repo
      service-plan: app-host
      service-name: RiskManagment-html5-apps-repo-host
